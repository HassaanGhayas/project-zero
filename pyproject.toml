[project]
name = "project-zero"
version = "0.1.0"
description = "AI FTE - Autonomous Digital Employee powered by Claude Code and Obsidian"
requires-python = ">=3.12"
dependencies = [
    "watchdog>=4.0.0",
    "python-dotenv>=1.0.0",
    "pyyaml>=6.0.3",
    "google-auth>=2.28.0",
    "google-auth-oauthlib>=1.2.0",
    "google-api-python-client>=2.118.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "black>=24.0.0",
]
